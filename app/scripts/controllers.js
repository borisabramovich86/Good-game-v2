angular.module('mainApp.controllers', [])
    .controller('MainController', [
        '$rootScope',
        '$scope',
        function ($rootScope,
                  $scope) {

            console.log('Load mainController!');

            var self = this;
        }]);